{
  "name": "convo-messenger",
  "private": true,
  "version": "2.0.0",
  "description": "Secure end-to-end encrypted 1:1 and group messaging app built on BSV overlays",
  "scripts": {
    "lars": "lars",
    "cars": "cars",
    "lars:config": "lars config",
    "start": "lars start",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:lint": "npm --prefix frontend run lint",
    "frontend:test": "npm --prefix frontend run test",
    "broker:test": "npm --prefix rtc-broker run test",
    "verify": "npm run broker:test && npm run frontend:lint && npm run frontend:test && npm run frontend:build",
    "build": "cars build 1",
    "predeploy": "cars project ensure-balance 1 --min 1 --topup-to 235000",
    "deploy": "cars release now 1"
  },
  "keywords": [],
  "author": "",
  "license": "SEE LICENSE IN LICENSE.txt",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@p2ppsr/cars-cli": "^1.2.31",
    "@p2ppsr/lars": "^1.5.12"
  }
}
